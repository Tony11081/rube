# Reduce memory usage and optimize for CI
engine-strict=false
fund=false
audit=false
prefer-offline=true
maxsockets=1
progress=false
loglevel=error

# Cache settings
cache-max=86400000

# Registry settings
registry=https://registry.npmjs.org/

# Lockfile settings
package-lock=true
package-lock-only=false